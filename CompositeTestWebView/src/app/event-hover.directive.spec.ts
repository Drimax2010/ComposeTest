import { EventHoverDirective } from './event-hover.directive';

describe('EventHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new EventHoverDirective();
    expect(directive).toBeTruthy();
  });
});
